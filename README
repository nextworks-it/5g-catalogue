Nextworks 5G App and Service Catalogue

For setting up the environment run: 
./bootstrap.sh

For compiling the Catalogue application:
cd Catalogue
mvn clean package (-DskipTests for skipping tests)

For running the Catalogue application:
cd Catalogue
mvn spring-boot:run

For running properly the webgui, submodules are required: 
git submodule update --init
http://localhost


